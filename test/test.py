import requests
import json

import book
import user
import favorite

if __name__ in '__main__':
    #user.register()
    token=user.login()
    #book.create(token)
    #_id=book.myBooks(token)
    #_id=_id[0]["_id"]
    #book.getBookByIsbn()
    #book.deleteBookById(token)
    #book.update(token, _id)
    #favorite.create(token, _id)
    #favorite.myBooks(token)
    #favorite.delete(token, _id)